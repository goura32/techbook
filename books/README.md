# Books

このディレクトリには、各書籍を `books/<book-id>/` の形で配置します。

## 現在の内容

- `_template/`: 新しい書籍を追加するときの雛形

## 追加手順

1. `_template/` を複製して新しい `book-id` を付ける
2. `outline.md` と `target-reader.md` を先に埋める
3. 本文を書く前に `shared/common-explanations.md` と他書の `outline.md` `status.md` を確認する
