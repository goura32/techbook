# Common Explanations

複数の書籍で共通して必要になる説明を、このファイルに集約します。

## 運用ルール

- 各書籍で同じ背景説明や定義を繰り返し書かず、このファイルに抽出する
- 書籍本文では、共通説明を要約して参照し、その書籍固有の観点に集中する
- 他書の `outline.md` と `status.md` を確認して、説明の重複や役割衝突を避ける

## 現在の共通説明候補

- 開発環境の再現性を高める基本原則
- パッケージ管理と依存関係管理の考え方
- 設定ファイル、環境変数、シークレット管理の基本
- ログ、エラー、終了コードの基本
- テストの層と自動化の考え方
- CLI と API の役割分担
- チーム開発での変更管理とレビューの基本
- セマンティックバージョニングと互換性の考え方

## 直近の重複注意ポイント

- `TypeScript` と `Python` では、テスト、依存関係管理、設定管理の一般論が重複しやすい
- `Git & GitHub` と `Hermes Agent` では、レビュー、権限制御、運用ルールの基礎説明が重複しやすい
- `Hermes Agent` では Git 操作やプロジェクト文脈ファイルの説明が入りやすいため、Git 固有の詳細は `Git & GitHub` 側へ寄せる

## 共通説明ドラフト

### 境界で入力を扱う基本原則

複数の実務書で繰り返し現れるのが、「外部入力はそのまま信用しない」という原則です。API レスポンス、フォーム入力、環境変数、設定ファイル、CLI 引数は、見た目には構造化されていても、受け取る側から見ると未検証の入力です。

基本の流れは次の 3 段階です。

1. 生の入力を受け取る
2. 検証と変換を行う
3. 内部で使う安定した型や構造へ落とす

この原則は TypeScript、Python、Node.js、CLI、設定管理など、複数の書籍で共通して使えます。各書籍ではこの一般論を繰り返すのではなく、それぞれの言語やツールでどこへ実装責務を置くかに集中します。

## 候補メモ

- 技術選定の比較観点
- 開発環境セットアップの共通知識
- Git ワークフローの基本
- テスト戦略の基本用語
- パッケージ管理と依存関係管理の基本
- セマンティックバージョニングの基本
- API と CLI の設計原則
- ログ、設定、環境変数の扱い方
